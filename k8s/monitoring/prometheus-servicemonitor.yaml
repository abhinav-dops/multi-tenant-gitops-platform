apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tenant1-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - tenant1
  selector:
    matchLabels:
      tenant: tenant1
  endpoints:
    - port: http
      interval: 30s